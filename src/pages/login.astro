---
import Login from "@/components/Login";
import Layout from "@/layouts/Layout.astro";

const cookie = Astro.cookies.get('sk_identity');
if (cookie) return Astro.redirect('/');
---

<Layout title="Log in — Drains" description="Log in to Drains, your local-first engineering log.">
	<Login client:load />
</Layout>
