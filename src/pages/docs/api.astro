---
import DocsLayout from '@/components/DocsLayout.astro'
---

<DocsLayout title="API tokens" description="How Drains API tokens authenticate requests, attribute agent-written entries, and get revoked.">
  <p>
    API tokens authenticate requests made on your behalf by something other than your own signed-in
    browser — an MCP-connected agent, a script, or any external tool. They're account-scoped, not
    tied to a single drain, and can be created or revoked at any time from your avatar menu in the
    sidebar → <strong>API tokens</strong>.
  </p>

  <h2>Authenticating requests</h2>
  <p>Send the token as a bearer token on the <code>Authorization</code> header:</p>

  <pre><code>{`Authorization: Bearer <your-token>`}</code></pre>

  <h2>Attribution</h2>
  <p>
    Entries created with a token are never indistinguishable from who wrote them — they carry a
    <code>via &lt;token name&gt;</code> attribution visible in the entry's history, the same way a
    human-authored entry shows who wrote and last edited it.
  </p>

  <h2>Revoking access</h2>
  <p>
    Revoking a token immediately invalidates it for all future requests. Entries it already
    created are untouched — attribution is a permanent record, not a live reference to the token.
  </p>

  <p>
    For the full list of what an authenticated agent can do, see
    <a href="/docs/mcp">Connecting an agent</a>.
  </p>
</DocsLayout>
